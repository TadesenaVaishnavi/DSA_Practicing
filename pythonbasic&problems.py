WHy f"{num}-{ch}"?
f"" -- f-string (formatting enabled)
The f before the string allows you to write variables inside {} directly.

other way
1)str(num) + "-" + ch --- "" or '' okay
2) "{}-{}".format(num, ch)

for space ( - ) give direct space no need to add anything
